append(): inserts a single element into an existing list
# my_list = ['geeks', 'for', 'geeks']
# another_list = [6, 0, 4, 1]
# my_list.append(another_list)
# print my_list

bytes.fromhex(): convert hex string to bytes
hex_string = "1a2b3c"
bytes_result = bytes.fromhex(hex_string)
 
print(type(hex_string))
print(bytes_result)
print(type(bytes_result))

chr(): returns a string representing a character whose Unicode code point is the integer specified.
# num = 97
# print("ASCII Value of 97 is: ", chr(num))


join(): used to join elements of a sequence separated by a string separator
# # Joining with empty separator
# list1 = ['g', 'e', 'e', 'k', 's']
# print("".join(list1))

# # Joining with string
# list1 = " geeks "
# print("$".join(list1))

ord(): returns the Unicode code from a given character.
# print(ord('a')) # Output: 97
# print(ord('€')) # Output: 8364


zip(): used to combine two or more lists
# name = [ "Manjeet", "Nikhil", "Shambhavi", "Astha" ]
# roll_no = [ 4, 1, 3, 2 ]

# # using zip() to map values
# mapped = zip(name, roll_no)

# print(set(mapped))

